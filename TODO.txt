# earleybird TODO

## Bootstrap Grammar Verification

- [ ] Implement Rust macro for embedding iXML specs with bootstrap implementations (src/ixml_bootstrap.rs) #HIGH
  - [ ] Design procedural macro to annotate bootstrap rule definitions with iXML specs
  - [ ] Auto-generate round-trip tests comparing hand-coded vs parsed grammar behavior  
  - [ ] Create verification that bootstrap implementation matches embedded iXML specification
  - [ ] Consider declarative grammar builder macro as alternative approach

## Enhanced Debugging & Transparency

### Phase 1: CLI & Logging (Complete)
- [x] Add --verbose levels to cmd_parse command
- [x] Create centralized debug logging module with structured levels
- [x] Enhance Earley parser logging with key decision points
- [x] Write Earley trace to external file and make it easily greppable including by tools
- [x] Add grammar tree processing debug output (src/grammar.rs with GRAMMAR| prefix)
- [x] Enhanced parse failure diagnostics with detailed error reporting (src/parser.rs:580-634)
- [x] Hand-built vs parsed grammar comparison testing methodology

### Phase 2: Visual Debugging (Future)
- [ ] HTML trace viewer for step-by-step parse visualization
- [ ] Earley chart state visualization 
- [ ] Parse tree diff viewer for comparing expected vs actual
- [ ] Interactive parser stepper (step through one operation at a time)
- [ ] Export parse states to JSON/GraphViz formats

## Immediate Priorities - Quick Test Suite Wins (Target: 35% → 65% pass rate)

### Priority 1: Bootstrap Grammar Parsing Issues #CRITICAL
- [x] Complete character set member processing (src/grammar.rs:320,351) #HIGH
  - Status: COMPLETED - Added range support to `process_member_element` function
  - Added support for `range` child elements with `from`/`to` attributes
  - Tree processing now handles ranges, hex members, and class members correctly
- [x] Enhanced parse failure diagnostics (src/parser.rs:580-634) #HIGH
  - Status: COMPLETED - Added detailed parse failure reporting
  - Shows furthest position reached, partial completions, nearby completions, active queue
  - Dramatically improves debugging of bootstrap grammar parsing failures
- [x] Fix Earley parser ambiguity handling for character ranges #CRITICAL
  - Fixed: `["A"-"C"]` patterns now work correctly with proper queue management
  - Fixed: Parser no longer prioritizes early string completion over range exploration
  - Fixed: Simple hash system prevents task deduplication issues
  - Verified: All character range patterns working: ["A"-"C"], ["0"-"9"; "A"-"F"], ["A"; "0"-"9"]
  - Impact: Character range parsing no longer blocks test suite progress
- [x] Add comment syntax to bootstrap grammar definition (src/ixml_bootstrap.rs:12,17,36,39) #HIGH
  - Fixed: Implemented comment: -"{", (cchar; comment)*, -"}" with nested comment support
  - Fixed: Added cchar: ~["{}"] rule for comment content
  - Fixed: Integrated comments into s and RS spacing rules
  - Verified: Nested comments working: {outer {inner} comment}

### Priority 2: Unicode Character Classes (half day) #HIGH
- [x] Implement basic Unicode categories in bootstrap grammar (src/ixml_bootstrap.rs:22-24) #HIGH
  - Status: COMPLETED - Unicode character classes L, Nd, Mn, Zs now working
  - Fixed: process_class_member now uses UnicodeRange enum instead of ASCII approximations
  - Fixed: Added support for 'code' attributes (not just 'class' attributes)
  - Verified: Manual tests show [L], [Nd], [Mn], [Zs] patterns working correctly
  - Impact: Unicode character class parsing now functional

### Priority 3: Medium Term Features #MED
- [x] Implement insertion syntax: `+` insertion for literal text (src/ixml_bootstrap.rs:92,265) #MED
  - Fixed: Bootstrap grammar parsing support completed
  - Note: Full output generation semantics pending
- [ ] Complete Unicode hex range support in character classes #MED
  - Add support for Unicode block names and hex ranges beyond basic categories

## Current Test Suite Status (Updated 2025-09-30)
- **Correct Tests**: **MAJOR BREAKTHROUGH** - 58/93 passing (62.4% pass rate)
  - **58 PASS** (62.4%) - More than doubled from Phase 1's 28/93 (30.1%)
  - **+30 additional tests now passing** - Position-bucketed queue fixed bootstrap parsing
  - **~20 Bootstrap Errors** - Remaining bootstrap issues (down from 65+ in Phase 1)
  - **~15 Fail** - Output format mismatches, mostly minor XML formatting
  - **Note**: Total test count lower (93 vs 108) due to temporarily commented unicode-version-check tests
- **Recent Major Fixes**:
  - **Architecture Simplification**: COMPLETED - removed all parent-hash/blockchain complexity from parser
  - **Task Deduplication**: SIMPLIFIED - single identity hash per task based on core fields
  - **Nullable Nonterminal Handling**: FIXED - proper advancement for deduplicated nullable nonterminals
  - **Code Clarity**: IMPROVED - much simpler parser structure for easier debugging
- **Unicode Character Classes**: WORKING - [L], [Nd], [Mn], [Zs] patterns parse correctly
- [x] **Left Recursion Issue**: COMPLETELY RESOLVED - implemented alternative indexing with simple deduplication #CRITICAL
  - Problem: Parent-hash chaining created infinitely growing chains for predictions (expr→sum→expr = [A→B→C→D...])
  - Root Cause: Confusion between individual alternatives (Rules) vs grouped alternatives (BranchingRules)
  - Solution: Added `alt_index: usize` field to Task struct with simple deduplication strategy
  - Implementation: Basic identity hash based on name[alt_index], origin, pos, dot
  - Verification: `S: S, "a"; "b".` with input `ba` now parses correctly as `<S><S>b</S>a</S>`
  - Technical Details: Position semantics changed to S(N) notation, removed unused at_end() method, removed all parent-hash complexity
  - Files Modified: src/parser.rs (Task struct, have_we_seen method), src/debug.rs (trace format)
  - Impact: Left recursion fully working, no more infinite loops or 🚧 TODO markers in test results
- **Infrastructure Improvements**: Enhanced diagnostics, background task support, emoji documentation, simplified architecture

### Primary Issue: Bootstrap Grammar Parsing
- [x] **Implement nullability-based epsilon completion** #CRITICAL
  - Status: COMPLETED - Fixed epsilon completion logic in predict() function
  - Solution: Added `is_alternative_nullable()` method to check individual Rule nullability
  - Implementation: Modified predict() function to trigger immediate completion for nullable alternatives
  - Impact: Significant progress - bootstrap parsing now reaches further positions (5→8+ vs previous 6)
  - Files: src/parser.rs (predict function), src/grammar.rs (is_alternative_nullable method)
  - Verification: Simple epsilon cases like `'a: .'` now work correctly
- [x] **Validate plus-separated synthetic rules are working** #HIGH
  - Status: COMPLETED - Created and tested minimal plus-sep case
  - Result: Plus-separated rules (repeat1_sep) work perfectly for simple cases
  - Evidence: `item++sep` pattern correctly parses "a a" as `<root><item>a</item><sep> </sep><item>a</item></root>`
  - Conclusion: The issue is NOT in the core plus-separated logic
- [ ] **Investigate remaining bootstrap parsing issue** #CRITICAL
  - Issue: 67/108 tests still fail at bootstrap grammar parsing stage (confirmed with Phase 1 changes)
  - Root Cause: NOT in plus-sep logic or epsilon completion - likely task ordering issue that position-bucketed queue will fix
  - Pattern: Simple grammars work (e.g., `'a: "x".\nb: "y".'`) but complex ones fail (e.g., `'a: b. b: "x".'`)
  - Next: Implement position-bucketed queue as this may resolve the fundamental parsing order issue

## Lower Priority - Architecture
- [ ] Implement Grammar::to_string() method for bootstrap self-validation #MED
  - Goal: Serialize internal Grammar representation back to valid iXML syntax
  - Use case: Ultimate bootstrap smoke test - parse bootstrap grammar with itself
  - Implementation: Walk grammar structure and emit proper iXML syntax with marks, alternatives, etc.
  - Value: Would enable `Grammar::from_ixml_str(&bootstrap_grammar.to_string())` roundtrip test
  - Complexity: Significant - need to handle all grammar constructs, marks, repeats, character classes
- [ ] Investigate test suite debug configuration integration #LOW
  - Add --verbose and --trace-file support to `eb suite` command
  - Would help with debugging complex conformance failures
- [x] Follow Rust testing conventions throughout codebase #LOW
  - Use `#[cfg(test)]` modules in source files for unit tests
  - Reserve `tests/` directory for integration tests only
  - Ensures tests stay close to the code they test
- [ ] Add SUMMARY log level for high-level debugging #HIGH
  - Implement SUMMARY level (above INFO) with emoji indicators
  - Focus on major parsing stages and outcomes only
  - Example: "🔍 SUMMARY: Grammar parsed ✅ | Input parsing ❌ | Character sets: hex✅ string❌"
  - Extremely token-efficient for debugging workflows
- [ ] Replace ad-hoc logging with structured tracing crate #MED
  - Use `tracing` crate with spans and structured fields
  - Add contextual information to all parsing operations
  - Enable hierarchical debugging with proper instrumentation
  - Self-documenting error types with source location info
- [ ] Improve parse error reporting with expected token information (src/parser.rs) #MED
  - Problem: Current errors like "no completed parse" are too generic and unhelpful
  - Solution: Track SCANNER failures at furthest position reached during parsing
  - Implementation: Collect unique expected tokens/characters at failure point
  - Format: "Parse failed at position 15: expected one of ['.', ')', 'digit'] but found 'x'"
  - Benefits: Works for both bootstrap ixml parsing and target grammar parsing
  - Considerations: Limit expectation set size (max 5-10), avoid noise from irrelevant backtrack attempts
  - Technical: Enhance ParseError struct with expected_tokens HashSet and actual_char fields
  - Files: src/parser.rs (ParseError, error collection in SCANNER), src/debug.rs (error formatting)